
{
  "name": "react-native-inner-printer",
  "version": "0.0.7",
  "description": "React native library to print in android tablets with embedded printers",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "react-native"
  ],
  "author": "@josesuero",
  "license": "MIT",
  "peerDependencies": {
    "react-native": "^0.57.8"
  }
}
